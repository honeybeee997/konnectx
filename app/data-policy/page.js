import React from "react";

import DataPolicy from "@/components/Components/DataPolicy";

export const metadata = {
  title: "Data Policy",
};

const Page = () => {
  return <DataPolicy />;
};

export default Page;
