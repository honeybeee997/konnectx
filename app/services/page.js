import React from "react";

import Services from "@/components/Components/Services";

export const metadata = {
  title: "Services",
};

const Page = () => {
  return <Services />;
};

export default Page;
