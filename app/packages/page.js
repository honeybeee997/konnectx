import React from "react";

import Packages from "@/components/Components/Packages";

export const metadata = {
  title: "Packages",
};

const Page = () => {
  return <Packages />;
};

export default Page;
